# ================================================================
# SSL4MIS - Environment Variables Template
# Sao chép file này thành .env và điền thông tin thực tế
#   cp .env.example .env
# QUAN TRỌNG: Không commit file .env lên git!
# ================================================================

# ============ SCP / VPS ============
# Cách lấy: xem trong email từ nhà cung cấp VPS (Vultr, DigitalOcean, Linode...)
SCP_USER=root
SCP_HOST=123.456.789.000        # IP hoặc domain của VPS
SCP_PORT=22
SCP_DEST_PATH=/root/ssl4mis_weights
SCP_KEY=~/.ssh/id_rsa           # Để trống nếu dùng password

# ============ AZURE BLOB STORAGE ============
# Cách lấy: xem hướng dẫn bên dưới
AZURE_ACCOUNT=yourstorageaccount
AZURE_CONTAINER=ssl4mis-weights
# Chọn 1 trong 2:
AZURE_SAS_TOKEN=                # Paste SAS token vào đây (bắt đầu bằng ?sv=...)
AZURE_USE_LOGIN=false           # Hoặc set true nếu dùng `az login`
